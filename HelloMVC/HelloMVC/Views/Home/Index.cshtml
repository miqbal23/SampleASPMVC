@model IEnumerable<HelloMVC.Models.Person>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

Welcome to ASP.NET MVC. Here are some data that we have

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Country)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Country)
        </td>        
    </tr>
}

</table>
